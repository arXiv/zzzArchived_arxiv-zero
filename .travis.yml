language: python
sudo: required
cache: pip
os:
  - linux
python:
  - "3.6"
services:
  - docker
script:
  - ./lintstats.sh
  - pip install -r requirements/test.txt
  - nose2 --with-coverage
after_success:
  - coveralls
